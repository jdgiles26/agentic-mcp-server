# DRIFT.md

Generated by `scripts/audit.mjs`. Re-run with `/audit` or it fires automatically on every `git commit`.

**Generated**: 2026-05-24T12:53:07.115Z  
**Commit**: 17cba41  
**Findings**: 2 HIGH, 5 MED, 7 LOW

Severity: HIGH = doc actively contradicts shipped behavior or wiring is broken. MED = stale fact that will mislead a reader. LOW = nit.

## HIGH

### HIGH-01 · docs

docs/packages.md does not document apps/mcp (the MCP server) at all — the package exists in code but is invisible to anyone reading the docs

**Evidence**: `docs/packages.md`

### HIGH-02 · docs

docs/architecture.md sequence diagram only covers the web flow; the MCP transports (stdio + HTTP + SSE) are missing entirely

**Evidence**: `docs/architecture.md:5-34`

## MED

### MED-01 · deps

@prompt-forge/patterns declares "zod" in dependencies but no src/* file imports it

**Evidence**: `packages/patterns/package.json`

### MED-02 · deps

@prompt-forge/web declares "@prompt-forge/patterns" in dependencies but no apps/web/src/* file imports it

**Evidence**: `apps/web/package.json`

### MED-03 · logger

docs/development.md:51 says "Use createLogger('scope-name'). Never console.log directly outside the logger." — but no production code actually calls createLogger() anywhere

**Evidence**: `packages/core/src/logger.ts is exported but unwired`

### MED-04 · tests

README claims "169 hermetic tests" but actual vitest count is 178

**Evidence**: `README.md:19`

### MED-05 · tests

docs/tdd-strategy.md:18 says E2E runs against a real local Ollama, but apps/web/e2e/enhance.spec.ts mocks /api/enhance via page.route — no real LLM is contacted

**Evidence**: `docs/tdd-strategy.md:18 vs apps/web/e2e/enhance.spec.ts:38-50`

## LOW

### LOW-01 · deps

next.config.mjs lists "@prompt-forge/patterns" in transpilePackages but it is unused

**Evidence**: `apps/web/next.config.mjs`

### LOW-02 · docs

docs/development.md:21 suggests "pnpm dev" but the root package.json has no "dev" script

**Evidence**: `docs/development.md:21`

### LOW-03 · docs

docs/development.md:89 references "pnpm --filter @prompt-forge/enhancer test:watch" but that script does not exist

**Evidence**: `docs/development.md:89`

### LOW-04 · docs

docs/development.md:95 suggests "pnpm format" but the root package.json has no "format" script

**Evidence**: `docs/development.md:95`

### LOW-05 · docs

docs/tdd-strategy.md:63 references "pnpm --filter @prompt-forge/enhancer test:watch" but that script does not exist

**Evidence**: `docs/tdd-strategy.md:63`

### LOW-06 · stack

docs/tdd-strategy.md lists Zustand internals as a skipped concern but the repo has no Zustand dependency or usage

**Evidence**: `docs/tdd-strategy.md:26`

### LOW-07 · tooling

docs/development.md pins pnpm 9.12.0 but root package.json declares pnpm@10.33.2

**Evidence**: `docs/development.md:8 vs package.json:15`

---

## What this script checks

1. Declared deps in package.json vs actual imports under src/
2. Catalog size claim vs `grep slug: packages/patterns/src/catalog.ts`
3. Test-count claims in README vs live `pnpm -r test` output
4. Doc references to `pnpm <script>` vs the actual scripts in every package.json
5. Every `ProviderKind` in code appears in docs/providers.md
6. `apps/mcp` is mentioned in docs/packages.md and docs/architecture.md
7. Dependency-graph claims in docs/architecture.md match real imports
8. `createLogger` is wired into production code if docs say it should be
9. Stack components named in docs/tdd-strategy.md actually exist in the repo
10. pnpm version pin in docs vs root package.json
11. E2E claim "real local Ollama" vs whether the spec mocks /api/enhance

Things this script does NOT check: prose accuracy of directive text, mermaid diagram correctness,
whether tests actually test the right thing, runtime behavior of the live system.
