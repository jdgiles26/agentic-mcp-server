# DRIFT.md

Generated by `scripts/audit.mjs`. Re-run with `/audit` or it fires automatically on every `git commit`.

**Generated**: 2026-05-27T06:56:46.389Z  
**Commit**: 69248a5  
**Findings**: 0 HIGH, 0 MED, 0 LOW

Severity: HIGH = doc actively contradicts shipped behavior or wiring is broken. MED = stale fact that will mislead a reader. LOW = nit.

---

## What this script checks

1. Declared deps in package.json vs actual imports under src/
2. Catalog size claim vs `grep slug: packages/patterns/src/catalog.ts`
3. Test-count claims in README vs live `pnpm -r test` output
4. Doc references to `pnpm <script>` vs the actual scripts in every package.json
5. Every `ProviderKind` in code appears in docs/providers.md
6. `apps/mcp` is mentioned in docs/packages.md and docs/architecture.md
7. Dependency-graph claims in docs/architecture.md match real imports
8. `createLogger` is wired into production code if docs say it should be
9. Stack components named in docs/tdd-strategy.md actually exist in the repo
10. pnpm version pin in docs vs root package.json
11. E2E claim "real local Ollama" vs whether the spec mocks /api/enhance

Things this script does NOT check: prose accuracy of directive text, mermaid diagram correctness,
whether tests actually test the right thing, runtime behavior of the live system.
